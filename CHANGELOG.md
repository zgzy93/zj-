# 更新日志

## v2.1 — 2026-06-10

### ✨ 新增内容
- **计组期末复习综合题库**：基于 4 个期末复习文件导入 275 题，覆盖全书
  - 选择题 90 道（`danxuan`）
  - 判断题 90 道（`panduan`）
  - 填空题 60 道（`tiankong`）
  - 简答题 35 道（`jianda`，含名词解释）
- 新增计组第999章「期末复习综合」（ch5），可作为全书综合复习使用

### 📁 文件调整
- `data/jz_ch5.js`：新增期末复习综合题库（275 题）
- `index.html` / `quiz-mobilenew.html` / `quiz-pc.html`：`_MANIFEST_DATA` 同步添加 ch5
- `parse_review.py`：期末复习 txt → JS 转换脚本（本地工具）

## v2.0 — 2026-06-05

### ✨ 新增功能
- **新科目 Hadoop**：新增 Hadoop 科目，含 50 道单选、20 道多选、10 道填空，共 80 题
- **新题型 多选题**：支持多选题（`duoxuan`）的完整适配——切换选择、确认提交、正误反馈
- **多选题 PC 快捷键**：`1~4` 切换选中的选项、`Enter` 提交答案

### 📁 文件调整
- `data/hd_ch1.js`：新增 Hadoop 题库数据文件
- `data/extract_hadoop.py`：Hadoop 题库提取脚本（已加入 `.gitignore`）
- 所有三端（`index.html` / `quiz-pc.html` / `quiz-mobilenew.html`）同步更新
- `CLAUDE.md`：题型表、文件结构等文档同步更新

### 🔧 技术细节
- 多选题状态通过 `state.selectedOptions`（数组）管理，支持选项切换/取消
- 答案比对使用字母排序后全等比较（忽略顺序）
- 多选题结果展示三种视觉状态：✅正确选中（绿色）、❌错误选中（红色）、⚠️漏选（琥珀色虚线）
- 所有状态重置点新增 `selectedOptions=null` 清理

### 🐛 Bug 修复
- **PC 端无法点击**：修复 `switchCh` 函数声明被截断导致 JS 解析失败的严重 bug
- **多选题缺"查看答案"按钮**：新增 `📌 查看答案` 按钮，支持直接看答案
- **查看答案后误判"答案有误"**：`handleShowAns` 清空 `selectedOptions`，区分提交/查看模式
- **漏选样式与 PC hover 冲突**：`.opt.ms` 补充 `cursor:default`，PC 版增加 `.opt.ms:hover` 覆盖

## v1.4 — 2026-06-04

### ✨ 新增功能
- **触屏滑动切换题目**：手机端支持左滑下一题、右滑上一题，手势识别准确流畅
- **题型筛选同步补齐**：`quiz-mobile.html` 新增题型筛选（单选/判断/填空/简答/计算），章节选择页和答题页均可切换
- **题号快速跳转同步补齐**：`quiz-mobile.html` 导航栏支持点击题号输入后回车跳转

### 🔧 优化
- **滑动区域覆盖全屏**：不仅在题目卡片上滑动有效，触摸下方空白区域同样可触发切换（`index.html` / `quiz-mobile.html`）
- **滑动动画更平滑**：手指滑动时卡片跟随，松手后旧卡片顺势滑出屏幕，新卡片从对侧滑入，衔接自然
- **功能一致化**：`quiz-mobile.html` 补全题型筛选、题号跳转、随机缓存、空状态提示，与 `index.html` 功能对齐
- 动画使用 `cubic-bezier` 缓动曲线，手感流畅

### 📁 文件调整
- **新增 `quiz-mobilenew.html`**：`index.html` 的完整副本，作为独立的手机端入口
- **`quiz-mobile.html` 停止维护**：不再同步更新，保留为历史版本
- **新增 `CLAUDE.md`**：项目介绍书，方便后续开发者快速上手

## v1.3 — 2026-06-04

### ✨ 新增功能
- **每章题型筛选**：在章节选择页和答题页均可通过题型芯片（单选/判断/填空/简答/计算）筛选题目，点击即可切换显示的题型
- 筛选状态实时影响题目总数和章节题目数

## v1.2 — 2026-06-04

### ✨ 新增功能
- **题号快速跳转**：做题时可直接点击导航栏中的题号数字，输入目标题号后按回车快速跳转（`index.html` 移动端 + `quiz-pc.html` PC端同步支持）
- **PC 独立版本**：新增 `quiz-pc.html`，专门针对大屏优化，支持顶部标签切换、键盘快捷键（← → 翻题、1~4 选选项、Space 看答案、S 收藏、R 随机）

### 🔧 优化
- 数据加载改为动态 `<script>` 标签注入，兼容 `file://` 本地直接打开
- 答题数据独立存储到 `localStorage`，支持多科目隔离

---

## v1.1 — 2026-06-04

### ✨ 新增功能
- **多科目体系**：支持 SPA 科目切换架构，data 目录按 `科目_章节.js` 组织
- **题库浏览页**：🔍 模式下可查看总统计、各章节进度
- **重置选项细化**：「清空记录」只清除答题历史，保留错题和收藏；「重置所有」单独清除全部数据

### 🔧 优化
- 题库数据从内嵌 JSON 重构为外置独立 JS 文件（`data/*.js`），便于维护和扩展

---

## v1.0 — 2026-06-04

### ✨ 首次发布
- 计算机组成原理刷题系统上线
- 支持单选题、判断题、填空题、简答题、计算题五种题型
- 提供章节刷题、全部刷题、随机刷题、错题本四种模式
- 答题结果即时反馈，错题自动收录
- 支持收藏题目（⭐）
- 手机版（`quiz-mobile.html`）与 PC 版同步上线，共 364 道题
- 数据通过 `extract_data.py` 从原始文本提取
